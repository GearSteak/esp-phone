/*
 * Placeholder — replaced when assets/icons/*.png are converted to LVGL arrays.
 * Keep UI_USE_APP_ICONS 0 until real assets exist.
 */
#include "Config.h"

#if UI_USE_APP_ICONS
#include <lvgl.h>
// Include generated descriptors here, e.g.:
// #include "icons/icon_phone.c"
#error "UI_USE_APP_ICONS is 1 but no icon data yet — convert PNGs first"
#endif
