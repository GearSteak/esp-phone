# Digivice 4×4 T9 keypad (optional / unused)

**Not the default.** Primary nav is **7 hard buttons** — see [`DIGI_BUTTONS.md`](DIGI_BUTTONS.md).

Keep `t9_keypad_inputd.py` only if you later want multi-tap typing; disable `digi-buttons-inputd` first (GPIO conflict on the default pins).
